# Docker Container with RabbitMQ server image

### Steps for usage:
Start detached container: \
```docker-compose up -d```

Enable some plugin: \
```docker exec -it [container-name] rabbitmq-plugins enable [plugin-name]```

[See more](https://phoenixnap.com/kb/deploy-postgresql-on-docker)
